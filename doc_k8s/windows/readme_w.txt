 _           _
/!\ WARNING /!\

Before using a script: 
	Dockerfile.bat 
	Kubernetes_deployment.bat:

- Make sure the parameters in the Dockerfile.bat are the right ones:
           is the URL of the registry correct?
- Make sure Docker works on the machine