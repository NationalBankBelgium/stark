 _           _
/!\ WARNING /!\

Before using a script: 
	Dockerfile.script
	Kubernetes_deployment.script

- Execute Kubernetes_install.script in first to install Docker and Kubernetes
- Make sure the parameters in the Dockerfile.script are the right ones:
           is the URL of the registry correct?
- Make sure Docker works on the machine